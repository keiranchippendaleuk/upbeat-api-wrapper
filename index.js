const { getStats } = require('./libs/stats');
const { getHistory } = require('./libs/history');
const { getBooked } = require('./libs/booked');

module.exports = {
    getStats,
    getHistory,
    getBooked
};
